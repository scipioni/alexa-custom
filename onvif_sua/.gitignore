# Secrets — camera / scan / web GUI passwords. Only .env.example is committed.
.env
# Private package registry credentials (task publish). Only .pypi.env.example is committed.
.pypi.env
.claude
.opencode
.venv
data/alarm.json
data/events.json
