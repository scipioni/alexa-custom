## 1. Config (ONVIF_SUA)

- [x] 1.1 Add `serena_*` default keys to the runtime `_cfg` in `onvif_sua/config.py` (env-defaulted): `serena_enabled` (`SERENA_ENABLED`, default false), `serena_topic_prefix` (`SERENA_TOPIC_PREFIX`, default `alexa`), `serena_node_id` (`SERENA_NODE_ID`, default ``), `serena_command_template` (`SERENA_COMMAND_TEMPLATE`, default `caduta {cam_name}`), `serena_announce_ready` (`SERENA_ANNOUNCE_READY`, default true), `serena_announce_template` (`SERENA_ANNOUNCE_TEMPLATE`, default `sensore uomo a terra {cam_name} attivo`), `serena_announce_fault` (`SERENA_ANNOUNCE_FAULT`, default false), `serena_announce_fault_template` (`SERENA_ANNOUNCE_FAULT_TEMPLATE`, default `attenzione, sensore uomo a terra {cam_name} non attivo`), `serena_announce_fault_interval_s` (`SERENA_ANNOUNCE_FAULT_INTERVAL_S`, default 300), `serena_announce_fault_grace_s` (`SERENA_ANNOUNCE_FAULT_GRACE_S`, default 60), `serena_startup_alarm_max_age_s` (`SERENA_STARTUP_ALARM_MAX_AGE_S`, default 300 — recency window for adopting an in-progress fall at startup, task 3.5).
- [x] 1.2 Add an explicit boolean-coercion helper (`_coerce_bool`) and a `_BOOL_KEYS` set in `onvif_sua/config.py`; include `serena_enabled`, `serena_announce_ready`, and `serena_announce_fault` in it. Coercion rule: case-insensitive `1/true/yes/on` (and a native YAML `true`) → `True`; everything else (`0/false/no/off/""`) → `False`. Apply it to both the env defaults (do NOT store the raw string) and the YAML values, so a raw non-empty string is never truthy-by-accident. Add `serena_announce_fault_interval_s`, `serena_announce_fault_grace_s`, and `serena_startup_alarm_max_age_s` to the numeric-coercion set (`_INT_KEYS` or a float equivalent), clamped to ≥ 0, falling back to defaults (300 / 60 / 300) on a non-numeric value. Additionally clamp `serena_announce_fault_interval_s` up to a floor of 30 s (log a warning) so a `0`/near-zero interval cannot flood announcements. Do NOT clamp `serena_announce_fault_grace_s` (a `0` legitimately disables the suppression window), but log a warning at load when it is `0` noting that startup/flap false "non attivo" announcements become possible.
- [x] 1.3 Parse an authoritative `serena:` block in `_load_settings_yaml()` (same pattern as the `mqtt:` block), overriding the env defaults when keys are present, routing `enabled`/`announce_ready`/`announce_fault` through `_coerce_bool` and the interval/grace through numeric coercion.
- [x] 1.4 Validate `serena_command_template`, `serena_announce_template`, **and** `serena_announce_fault_template` at config load: each may reference only `{cam_name}`. On an unknown placeholder or malformed braces, log a warning and fall back to that field's default — probe with a dummy `format(cam_name="x")` inside try/except, or reject any `{...}` field name ≠ `cam_name`.
- [x] 1.5 Document the full `serena:` block (including `announce_ready`/`announce_template`, `announce_fault`/`announce_fault_template`/`announce_fault_interval_s`/`announce_fault_grace_s`, and `startup_alarm_max_age_s`) in `settings.yaml` (commented) and note in `.env.example` the corresponding `SERENA_*` env vars. Document the `announce_fault_interval_s` 30 s floor and that `announce_fault_grace_s: 0` disables the startup/flap suppression window (with the false-positive caveat).

## 2. Bridge publish helper (ONVIF_SUA)

- [x] 2.1 Add `_mqtt_publish_serena_trigger(cam_name: str)` in `onvif_sua/mqtt.py`: **first guard** `if not _mqtt_client: return` (mirror `_mqtt_publish` at `mqtt.py:92`); then no-op unless `serena_enabled`, `mqtt_host` set, and `serena_node_id` non-empty (log a warning if enabled but `node_id` empty).
- [x] 2.1a Add a `_serena_voice_name(name: str) -> str` helper in `onvif_sua/mqtt.py` (option B normalization): lowercase, replace `-`/`_` with a single space, collapse/trim whitespace. Use it wherever `{cam_name}` is rendered (trigger phrase + all announcements). If the normalized result is empty, fall back to the raw `name` (or the camera IP if that is empty too) and log a warning — never render an empty name into a phrase. Unit-covered by 5.1.
- [x] 2.1b Add a shared `_camera_working(ip) -> bool` helper in `onvif_sua/mqtt.py` implementing the canonical confirmed-working predicate: returns `True` when `_cameras[ip]["simulated"]`, else `bool(stream_alive) and rule_enabled is True` (read under `_lock`). Both `_refresh_detection_ok` (D6) and the periodic fault driver (task 3.4) MUST derive working/fault state from this one helper — neither may read raw `rule_enabled`/`stream_alive` in a way that bypasses the simulated override. This is the single source of truth that keeps the active and fault paths from disagreeing (fixes the simulated-camera false-fault bug).
- [x] 2.2 Build the phrase via `command_template.format(cam_name=_serena_voice_name(cam_name))` and publish to `f"{serena_topic_prefix}/{serena_node_id}/trigger/run"` with `qos=1, retain=False`, reusing the existing `_mqtt_client`. Wrap the phrase build + publish in `try/except Exception` that logs and returns — the helper SHALL NOT propagate any exception to the caller (so a bad template or transient publish error can never stall the detection loop via the catch-all at `worker.py:563`).
- [x] 2.3 Export the helper alongside the other `_mqtt_*` functions imported by `worker.py`.
- [x] 2.6 Add `_mqtt_announce_serena_active(cam_name: str)` in `onvif_sua/mqtt.py` for the startup announcement: same guards as 2.1 (`_mqtt_client` connected, `serena_enabled`, `mqtt_host`, `node_id` non-empty) **plus** no-op unless `serena_announce_ready`. Render `announce_template.format(cam_name=_serena_voice_name(cam_name))` and publish to `f"{serena_topic_prefix}/{serena_node_id}/tts/set"` with `qos=1, retain=False`. Wrap build+publish in the same `try/except Exception` (never propagate). Export it alongside the other `_mqtt_*` helpers.
- [x] 2.7 Add `_mqtt_announce_serena_fault(cam_name: str)` in `onvif_sua/mqtt.py` for the fault announcement: same base guards as 2.1 **plus** no-op unless `serena_announce_fault`. Render `announce_fault_template.format(cam_name=_serena_voice_name(cam_name))` and publish to `f"{serena_topic_prefix}/{serena_node_id}/tts/set"` with `qos=1, retain=False`, wrapped in the same `try/except Exception` (never propagate). Export it alongside the other `_mqtt_*` helpers.
- [x] 2.7a Add `_mqtt_announce_serena_recovery(cam_name: str)` for the fault-recovery notice: renders the **active** `announce_template` (recovery speaks "attivo") to `tts/set`, but is gated on `serena_announce_fault` — **NOT** `serena_announce_ready` — because recovery is part of the fault-tracking feature. Do NOT reuse `_mqtt_announce_serena_active` for recovery (it no-ops on `announce_ready=false`, which would silently swallow the recovery when `announce_ready:false, announce_fault:true`). Same base guards (`_mqtt_client` connected, enabled, `node_id`) + `try/except` (never propagate). Export alongside the other `_mqtt_*` helpers.
- [x] 2.4 **(A) Commissioning log.** When the bridge is enabled and configured, log the fully-resolved target topic `f"{serena_topic_prefix}/{serena_node_id}/trigger/run"` exactly once at init (e.g. in `_mqtt_init()` after the client is created, or a one-shot guard in the helper) so the operator can confirm it matches Serena's real `node_id` (Serena defaults `node_id` to its hostname, `alexa_custom/mqtt.py:26`).
- [x] 2.5 **(B) Emit-time connectivity check.** In `_mqtt_publish_serena_trigger`, before publishing check `_mqtt_client.is_connected()`; if false, log an **error** ("serena bridge: broker not connected, fall command may be lost") and still attempt the buffered publish. Capture the publish handle and `wait_for_publish(timeout=1)` — a small bound (≤ 1 s) because this runs synchronously on the per-camera detection read thread and must not stall the read loop; log an error on timeout/failure. (Confirms broker receipt only, not Serena receipt — keep all of this inside the 2.2 `try/except`.)

## 2b. Bridge health diagnostic sensor (ONVIF_SUA)

- [x] 2b.1 **(D)** Add `_mqtt_publish_serena_bridge_status()` in `onvif_sua/mqtt.py` reusing the existing HA discovery/state pattern (`_mqtt_publish_detect_discovery` / `_mqtt_publish_detection_ok`): publish a **retained** diagnostic entity (binary_sensor or sensor) whose state reflects `_mqtt_client.is_connected()` and whose attributes include `enabled`, the resolved target topic, and `node_id`. No-op when the bridge is disabled.
- [x] 2b.2 Publish the status once at init (after MQTT connect) and refresh it on each emit and on connect/reconnect (e.g. from the existing `on_connect` in `_mqtt_init`, `mqtt.py:72`). Keep it independent of Serena — it reports ONVIF-side config/connectivity only, never claims Serena received a command.

## 3. Wire into the fall-start edge (ONVIF_SUA)

- [x] 3.1 In `onvif_sua/worker.py`, inside the `if not alarm_active:` block of the `TumbleDetection action=start` handler (~line 521, right after `_mqtt_publish(cur, "on")`), call `_mqtt_publish_serena_trigger(cur)`.
- [x] 3.2 Confirm no other call site emits the Serena trigger (keepalive loop and connect-time republish at ~line 451 must NOT call it), preserving once-per-fall semantics.
- [x] 3.3 Wire the startup announcement to the **truthful** "rule actually enabled" transition, not stream-connect:
  - Add a per-camera `serena_announced: False` key to the camera state dict where `rule_enabled`/`detection_ok` are initialized (`worker.py:57-60`).
  - In `_refresh_detection_ok(ip)` (`onvif_sua/mqtt.py:166`), inside the existing locked block, compute `announce_now = (not cam.get("serena_announced")) and _camera_working(ip)` (task 2.1b — reuse the shared predicate, do NOT re-derive from raw `rule_enabled`/`stream_alive`); if true, set `cam["serena_announced"] = True` and capture `name`. **Outside** the lock (next to the existing `_mqtt_publish_detection_ok` call), `if announce_now: _mqtt_announce_serena_active(name)`.
  - This funnel is already invoked on every stream-liveness change (`_mark_stream_seen` → `_refresh_detection_ok`) and every rule-state change (`_apply_rule_state` → `_refresh_detection_ok`), so whichever of {stream-alive, rule-confirmed-True} completes the pair last fires the announcement. `_camera_working` gates strictly on `rule_enabled is True` (NOT `detection_ok == "on"`, which is also `on` while `rule_enabled == "unknown"`; see `_compute_detection_ok`, `mqtt.py:158`).
  - Once per camera per process: do not reset `serena_announced` on reconnect or rule flap. (The old per-thread `announced` flag / `worker.py:451` stream-connect hook is NOT used.)
  - **Coordinate with fault recovery (no double "attivo"):** when the fault driver (task 3.4) emits a recovery notice it MUST also set `cam["serena_announced"] = True`, so the first-confirm path here does not additionally fire for the same confirmed-working transition. The `not serena_announced` guard above is what enforces the "exactly one active announcement per transition" rule.
- [x] 3.4 Wire the **multi-shot fault announcement** via a periodic driver over **all configured cameras** (not only connected ones):
  - Add per-camera state keys (init alongside `serena_announced`): `serena_fault_since: None`, `serena_fault_last_ts: 0.0`, `serena_fault_announced: False`.
  - Choose the driver: reuse the 60 s alarm-keepalive loop **only if** it already iterates the full configured camera list; otherwise add a small dedicated periodic task (e.g. tick every `min(60, interval)`) that iterates `config` cameras. Note `_cameras[ip]` is created at worker start (`worker.py:110`) for every configured camera, so offline cameras have an entry (with `stream_alive=False, rule_enabled="unknown"`); iterate configured cameras and skip any whose `_cameras[ip]` entry does not yet exist. Read each camera's state under `_lock` via `_camera_working(ip)` (task 2.1b) plus auth-quarantine state; do the `time.time()` comparisons and MQTT emits **outside** the lock.
  - Per camera each tick, compute `working = _camera_working(ip)` (the shared predicate — this makes simulated cameras count as working and never faulty, matching D6):
    - **working**: if `serena_fault_since is not None` and `serena_fault_announced`, call `_mqtt_announce_serena_recovery(name)` once (recovery notice, gated on `announce_fault` — task 2.7a, NOT `_mqtt_announce_serena_active`) **and** set `cam["serena_announced"]=True` (suppress the D6 first-confirm double-announce per task 3.3); then reset `serena_fault_since=None`, `serena_fault_last_ts=0.0`, `serena_fault_announced=False`.
    - **not working**: if `serena_fault_since is None`, set `serena_fault_since=now` (enter episode, no emit yet). Else if `serena_announce_fault` and `now - serena_fault_since >= grace` and `now - serena_fault_last_ts >= interval`, call `_mqtt_announce_serena_fault(name)`, set `serena_fault_last_ts=now`, `serena_fault_announced=True`.
  - Keep fault state strictly per camera; never let one camera's episode affect another. The only cross-write to `serena_announced` is the recovery-coordination set above (to prevent the double "attivo").

- [x] 3.5 Wire the **startup adoption of an in-progress fall** (avoids a silent miss when a fall was active before ONVIF_SUA (re)started — the sole call-for-help path):
  - Preserve `alarm.json`'s `saved_at` at load: `_load_alarm_file` (`worker.py:293`) currently does `data.pop("saved_at", None)` — instead capture it into a module-level `_alarm_saved_at` (parsed epoch, or `0.0` on absence/parse error) before popping.
  - At the first-connect republish point (`worker.py:~451`, right after `_mqtt_publish(cur, _cam_alarms.get(cur, "off"))`), reusing the existing `first_attach` (`worker.py:383`) and `alarm_active` (`worker.py:380`) locals — no new debounce state:
    ```python
    if first_attach and not alarm_active and _cam_alarms.get(cur) == "on" \
       and (time.time() - _alarm_saved_at) <= config._cfg["serena_startup_alarm_max_age_s"]:
        alarm_active = True                 # adopt the persisted episode
        _mqtt_publish_serena_trigger(cur)   # emit exactly one command, as if a real start fired
    ```
  - Setting `alarm_active = True` adopts the episode so any incoming `action=start` hits the existing `if not alarm_active` guard (no double-emit), and a later `stop`→`_fire_stop` re-arms normally. `first_attach` (flipped to `False` at `worker.py:416`, and the reconnect loop is the same thread) guards against re-emit on reconnect.
  - Recency gate: skip the emit when `_alarm_saved_at` is older than `serena_startup_alarm_max_age_s` (stale — e.g. crash between `start` and `_fire_stop`, or person recovered during the outage), and when the persisted state is `"off"`/absent. Subject to the same enable/preconditions + fault-isolation as a normal fall command.

## 3b. Camera-name reserved-word constraint (ONVIF_SUA web + config)

- [x] 3b.1 Add a small validator (e.g. `_name_has_reserved_word(name)` → True if `"camera"` is a case-insensitive substring of the sanitized name) in the config/web layer. Note the current decision: **substring**, case-insensitive (also blocks `telecamera`). Add a companion `_name_voice_collision(ip, name)` → True if `_serena_voice_name(name)` equals the normalized voice name of any **other** already-configured camera (so `salotto-1` and `salotto_1` cannot coexist, since Serena distinguishes cameras only by the rendered phrase).
- [x] 3b.2 Enforce in `onvif_sua/web/routes.py` `api_rename_camera` (`:82`): right **after** the sanitization at `:90` and **before** the ONVIF `SetHostname` call (`:116`) — mirroring the name-conflict check placement (`:102`) — return `JSONResponse({"status":"error","detail":"Il nome non può contenere 'camera' (usa il nome della stanza, es. 'cucina')"}, status_code=400)` so the existing frontend surfaces it as an alert. Do not touch the device or `settings.yaml` on violation.
- [x] 3b.3 Enforce the same in `api_save_camera` (`:160`, around the empty-name check at `:176`) and, as the shared chokepoint, in `_config_add_camera` (returns `(False, msg)` → propagated as the alert detail by both callers). This covers any future caller. Enforce the voice-collision rule (`_name_voice_collision`) at the same two endpoints and in the `_config_add_camera` chokepoint, with a user-facing detail like `"Il nome '{name}' coincide (dopo normalizzazione) con un'altra telecamera"`.
- [x] 3b.4 At config load (`_load_settings_yaml` / camera-list parse), log a warning for any configured camera whose name contains `camera`, **and** for any pair of cameras whose normalized voice names collide (hand-edited YAML bypasses the web validation). Do not crash; the bridge simply derives an awkward/ambiguous voice name until renamed.
- [x] 3b.5 Confirm the frontend (`web/static/settings.js` / `index.js`) already renders the error `detail` from these endpoints as a visible alert (it does for the name-conflict case); adjust only if the rename/save error path differs.

## 4. Serena-side confirmation flow (config only, no Serena code)

- [x] 4.1 Author one `with_wake: false` trigger **per camera** in Serena's `conf/actions/user.yaml` (2+ cameras → 2+ triggers), each with `commands` matching that camera's emitted phrase in **normalized** form (e.g. `caduta cucina`, `caduta salotto` — lowercase, `-`/`_` → space) and a single `ask` action whose text names that sensor. Keep each trigger's `commands` in sync with the (normalized) camera name used by `command_template`.
- [x] 4.2 Fill `on_reply`: affirmative → `say` + `livekit_join` + `telegram` (`<room>` link); negative → `say "va bene, chiamata annullata"`.
- [x] 4.3 Fill outer `ask.on_else` with a nested `ask` (repeat the question); the nested `ask.on_else` → `say "non ho ricevuto risposta, chiamo aiuto"` + `livekit_join` + `telegram` (fail-safe call).
- [x] 4.4 Include the complete copy-paste example in this change's docs (README/docs section) so an operator can add it and hot-reload Serena.

## 5. Tests (ONVIF_SUA)

- [x] 5.1 Unit-test `_mqtt_publish_serena_trigger`: emits correct topic/phrase/qos/retain=False when enabled+configured; no-op when disabled, when `node_id` empty, when `mqtt_host` empty, and when `_mqtt_client` is `None`; template renders `{cam_name}`; a publish/format exception is swallowed (does not propagate) and does not prevent the caller's state publish.
- [x] 5.1c Unit-test `_serena_voice_name`: `Salotto` → `salotto`; `salotto-1` → `salotto 1`; `cucina_piano_terra` → `cucina piano terra`; idempotent on already-clean names; an all-separator name (`___`) falls back to the raw name/IP and logs (never empty). And test the reserved-word validator: `camera0`, `Camera 0`, `telecamera-2` → rejected; `cucina`, `salotto` → accepted. Test `_name_voice_collision`: renaming/saving `salotto_1` while `salotto-1` exists → rejected. Assert `api_rename_camera` / `api_save_camera` return an error (non-200) with a `detail` for both a `camera`-containing name and a voice-collision, and do not persist it, and that config-load logs a warning for a hand-edited reserved name and for a normalized-name collision.
- [x] 5.1a Unit-test config coercion/validation: `SERENA_ENABLED`/`SERENA_ANNOUNCE_READY` in {`false`,`0`,`no`,`off`,``} → false; in {`true`,`1`,`yes`,`on`, mixed case} → true; same for the YAML values. An invalid `command_template` or `announce_template` (unknown placeholder / malformed braces) logs a warning and falls back to its default. Numeric coercion: a non-numeric `announce_fault_interval_s`/`grace_s`/`startup_alarm_max_age_s` falls back to `300`/`60`/`300`; an `announce_fault_interval_s` below 30 is clamped up to 30 with a warning; `announce_fault_grace_s == 0` is preserved (not clamped) and logs a warning.
- [x] 5.1b Unit-test `_mqtt_announce_serena_active`: emits the rendered `announce_template` to `<prefix>/<node_id>/tts/set` (qos=1, retain=False) when enabled + `announce_ready` + configured + connected; no-op when `announce_ready` is false, when disabled, when `node_id` empty, or when `_mqtt_client` disconnected; exception is swallowed. Also unit-test `_mqtt_announce_serena_recovery`: emits the rendered `announce_template` when enabled + `announce_fault` + configured + connected **regardless of `announce_ready`** (fires with `announce_ready=false`); no-op when `announce_fault` is false, when disabled, when `node_id` empty, or when disconnected; exception swallowed. And `_camera_working(ip)`: True for simulated regardless of raw state; True iff `stream_alive and rule_enabled is True` otherwise; False for `rule_enabled` in {`"unknown"`, `False`}.
- [x] 5.2 Test the start-handler wiring: a single `action=start` emits exactly one Serena command; repeated starts while active emit none; a `stop` then a new `start` emits again (re-arm). Use the existing worker test harness / mock the MQTT client.
- [x] 5.3 Test multi-camera independence (2+ cameras): concurrent/overlapping falls on distinct cameras each emit their own distinct phrase exactly once; one camera being active does not suppress another; per-camera re-arm is independent.
- [x] 5.3a Test the startup-announcement wiring against `_refresh_detection_ok`: no announcement while `rule_enabled == "unknown"` (even though `detection_ok` may be `on`); no announcement when `rule_enabled is False`; exactly one announcement to `tts/set` when `stream_alive` and `rule_enabled` first become `True` together (regardless of arrival order); no re-announcement on stream reconnect or rule flap; each of 2+ cameras announces once with its own name; nothing published when `announce_ready` is false or the rule stays disabled.
- [x] 5.3b Test the multi-shot fault driver (use a fake clock / mock `time.time`, mock the MQTT helpers): a not-working camera emits NO fault within `grace`; emits the fault once after `grace`; repeats every `interval` while still not-working; is silent when `announce_fault` is false; on return to working after ≥1 fault, emits the recovery (active) announcement once and re-arms (a later fault after grace announces again); a camera confirmed within `grace` never emits a fault; covers a never-connected/offline camera (driver iterates all configured cameras); two cameras are independent (one faulting does not affect the other).
  - **Simulated camera (fixes false-fault bug):** a `simulated: true` camera is treated as working by `_camera_working` → the driver NEVER emits a fault for it (even with raw `rule_enabled="unknown"`), while D6 still announces it active.
  - **Recovery gating (RH2):** with `announce_ready=false, announce_fault=true`, a camera that faulted then recovers still emits the recovery active announcement once (via `_mqtt_announce_serena_recovery`, gated on `announce_fault`).
  - **No double "attivo" (RM1):** a camera down past grace (fault announced), then confirmed-working, emits exactly ONE active announcement across the transition (recovery), not two — assert the D6 first-confirm path is suppressed because recovery set `serena_announced=True`.
- [x] 5.3c Test the startup-adoption wiring (task 3.5; mock MQTT + `time.time`, seed `_alarm_saved_at`): `alarm.json` `"on"` saved within `serena_startup_alarm_max_age_s` → exactly one `_mqtt_publish_serena_trigger` on first connect, `alarm_active` becomes True, and a subsequent `action=start` and a reconnect do NOT re-emit; `"on"` saved older than the window → no emit; `"off"`/absent → no emit; after adoption, a `stop`→`_fire_stop` then a fresh `start` emits again (re-arm).
- [x] 5.4 Test observability (A/B/D): the resolved topic is logged once at init when enabled; an emit while `_mqtt_client.is_connected()` is false logs an error and does not raise; the retained bridge-status entity is published with state reflecting the (mocked) connection status and the resolved topic in its attributes.
- [x] 5.5 Run the ONVIF_SUA test suite and linters per its `Taskfile.yml`.

## 6. Docs

- [x] 6.1 Update `ONVIF_SUA/README.md`: document the `serena:` config block, the emitted `trigger/run` topic/phrase contract, the active `tts/set` announcement (`announce_ready`/`announce_template`, once per camera on confirmed-working), the opt-in multi-shot fault announcement (`announce_fault`/`announce_fault_template`/`announce_fault_interval_s`/`announce_fault_grace_s`, with recovery notice), the shared-broker + matching-`node_id` requirement, the camera-naming rule (no `camera` in the name; names are normalized to lowercase words for the phrase/announcements; use room names), and the fail-safe-on-no-answer behavior.
- [x] 6.1a Add a **commissioning verification** step: run `mosquitto_sub -t 'alexa/#'` on the broker while triggering a test fall and confirm the exact `<prefix>/<node_id>/trigger/run` phrase arrives; cross-check the resolved topic logged at startup (task 2.4) against Serena's actual `node_id`. Document the bridge-health diagnostic entity (task 2b) and note the residual limitation: ONVIF confirms delivery to the broker, not that Serena received/acted (no Serena-side ack by design).
- [x] 6.2 Cross-reference from Serena's docs/`.claude/CLAUDE.md` that fall alarms arrive via `trigger/run` and are handled purely in `conf/actions/user.yaml` (no Serena code).
