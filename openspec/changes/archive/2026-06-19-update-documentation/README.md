# update-documentation

Aggiornare tutta la documentazione per riflettere lo stato attuale del codice: architettura STT a modello singolo, backend GStreamer, profili audio, comandi CLI alexa-* e task, tutte le chiavi di configurazione con valori di default
