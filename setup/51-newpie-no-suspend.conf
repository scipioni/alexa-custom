# Prevent WirePlumber from suspending the NewPie ALSA source node.
#
# Keeps the analog-stereo capture node always active so GStreamer clients
# (pipewiresrc, pulsesrc) connect without waiting for WirePlumber to wake it
# from SUSPENDED. Without this, the first buffer after a restart is delayed.

monitor.alsa.rules = [
  {
    matches = [
      { node.name = "~alsa_input.usb-*_NewPie_*" }
    ]
    actions = {
      update-props = {
        session.suspend-timeout-seconds = 0
      }
    }
  }
]
